# ساخت فایل اجرایی ویندوز (PersianPayroll.exe)

## نکته مهم درباره ساخت
PyInstaller امکان **cross-compile** ندارد؛ یعنی فایل اجرایی ویندوز فقط **روی خود
ویندوز** ساخته می‌شود (نه روی لینوکس/مک). بنابراین ساخت نهایی `.exe` باید روی یک
کامپیوتر ویندوزی انجام شود. همه فایل‌های لازم برای این کار در همین مخزن آماده است.

## پیش‌نیاز
- ویندوز ۱۰ یا ۱۱
- نصب [Python 3.10+](https://www.python.org/downloads/) با تیک‌زدن **Add Python to PATH**

## مراحل ساخت (یک‌بار)
۱. کل پوشه پروژه را روی ویندوز کپی کنید.
۲. روی فایل **`build_exe.bat`** دوبار کلیک کنید.
   - پیش‌نیازها و PyInstaller به‌صورت خودکار نصب می‌شوند.
   - فایل اجرایی ساخته می‌شود و پوشه تحویل آماده می‌گردد.

## خروجی
پوشه‌ی آماده‌ی تحویل:

```
dist\PersianPayroll\
    PersianPayroll.exe        ← با دوبار کلیک اجرا می‌شود
    _internal\                ← کتابخانه‌ها و قالب‌ها (دست نزنید)
    README.md                 ← راهنمای کوتاه فارسی
    TEST_CHECKLIST.md         ← چک‌لیست تست دستی
```

> **مهم:** دیتابیس و بکاپ‌ها **داخل این پوشه ساخته نمی‌شوند**. محل ثابت داده‌ها:
> `%LOCALAPPDATA%\PersianPayroll\payroll.db` و `...\PersianPayroll\backups`.
> بنابراین ساخت دوباره یا حذف پوشه `dist` هرگز دیتابیس کاربر را پاک نمی‌کند.

کافی است کل پوشه‌ی `dist\PersianPayroll` را روی هر ویندوزی کپی کنید و
`PersianPayroll.exe` را اجرا کنید. نیازی به نصب پایتون روی آن سیستم نیست.

## اپلیکیشن پنجره‌ای (Desktop) به‌جای مرورگر
نقطه‌ورود فایل اجرایی، `desktop.py` است: Flask در پس‌زمینه اجرا می‌شود و
**pywebview** یک پنجره native ویندوز با عنوان «حسابداری نیروهای نصب» (۱۲۰۰×۸۰۰،
قابل maximize) باز می‌کند. مرورگر سیستم باز نمی‌شود.

- پیش‌نیاز پنجره: **WebView2 Runtime** که روی ویندوز ۱۰/۱۱ معمولاً از پیش نصب است.
  اگر نبود، از سایت مایکروسافت «Microsoft Edge WebView2 Runtime» را نصب کنید.
- حالت مرورگری/توسعه همچنان با `python run_web.py` یا `run.bat` در دسترس است.

## پنجره کنسول (پنجره مشکی)
در `PersianPayroll.spec` متغیر `CONSOLE` وجود دارد:
- برای **اولین تست** روی ویندوز `CONSOLE = True` بماند (اگر خطایی بود دیده شود).
- پس از اطمینان از اجرای درست، آن را به **`CONSOLE = False`** تغییر دهید و دوباره
  `build_exe.bat` را اجرا کنید تا هیچ پنجره مشکی باز نشود.
- در هر دو حالت، خطاها در فایل `desktop_error.log` کنار برنامه هم ثبت می‌شوند.

## جزئیات فنی
- نوع بسته: **onedir** (پوشه‌ای) برای اجرای سریع و رفع‌اشکال آسان‌تر.
- مسیر منابع (templates/static/schema.sql) داخل بسته، و مسیر داده‌های ماندگار
  در `payroll/paths.py` مدیریت می‌شود. داده‌ها در `%LOCALAPPDATA%\PersianPayroll`
  ذخیره می‌شوند (خارج از پوشه برنامه)، پس build/rebuild آن‌ها را پاک نمی‌کند.
- در اولین اجرا، اگر مسیر داده خالی باشد و یک دیتابیس قدیمی دارای اطلاعات
  (مثلاً `...\instance\payroll.db` کنار exe) پیدا شود، به‌صورت **امن وارد** می‌شود؛
  دیتابیس دارای اطلاعات هرگز با دیتابیس خالی بازنویسی نمی‌شود.
- هنگام هر اجرا و پیش از مهاجرت ساختار، یک **بکاپ خودکار** گرفته می‌شود.
- برای تغییر محل ذخیره داده‌ها می‌توانید متغیر محیطی `PAYROLL_HOME` را تنظیم کنید.

## تست پس از ساخت
پس از ساخت، طبق فایل `TEST_CHECKLIST.md` موارد زیر را آزمایش کنید: اجرای exe،
ورود admin/admin، ثبت نیرو/پروژه/هزینه/دریافت/پرداخت، خروجی Excel و PDF، بکاپ
دستی و خودکار، و بستن/بازکردن دوباره برنامه برای اطمینان از حفظ اطلاعات.
