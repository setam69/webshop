# پلتفرم حسابداری دستمزد — راهنمای استفاده

## اجرا
روی فایل **`PersianPayroll.exe`** دوبار کلیک کنید.
چند لحظه صبر کنید تا مرورگر به‌صورت خودکار روی نشانی زیر باز شود:

```
http://127.0.0.1:5000
```

اگر باز نشد، همین نشانی را دستی در مرورگر وارد کنید.
برای بستن برنامه، پنجره مشکی برنامه را ببندید.

> نیازی به نصب پایتون یا اینترنت نیست. همه چیز داخل همین پوشه است.

## ورود اولیه
- نام کاربری: `admin`
- رمز عبور: `admin`

پس از ورود، از **تنظیمات ← مدیریت کاربران** رمز را تغییر دهید.

## محل ذخیره اطلاعات (کنار همین برنامه)
- دیتابیس: پوشه‌ی **`instance\payroll.db`**
- بکاپ‌ها: پوشه‌ی **`backups\`**

با هر بار اجرا یا جایگزینی نسخه جدید، اطلاعات قبلی **پاک نمی‌شود**.
هنگام هر اجرا یک بکاپ خودکار در پوشه `backups` ساخته می‌شود.

## بکاپ و بازگردانی
- **بکاپ دستی:** تنظیمات ← بکاپ دیتابیس ← «تهیه بکاپ جدید».
- **بازگردانی:** برنامه را ببندید، فایل بکاپ موردنظر را از پوشه `backups` کپی کرده،
  در پوشه `instance` بگذارید و نام آن را به `payroll.db` تغییر دهید؛ سپس برنامه را باز کنید.

## انتقال به کامپیوتر دیگر
کل همین پوشه (`PersianPayroll`) را کپی کنید. دیتابیس و بکاپ‌ها همراه آن منتقل می‌شوند.

## گزارش‌ها
در صفحه گزارش‌ها می‌توانید خروجی **Excel** بگیرید یا با دکمه **«PDF / چاپ»**
از طریق مرورگر فایل PDF بسازید.

برای فهرست کامل تست‌های دستی، فایل `TEST_CHECKLIST.md` را ببینید.
