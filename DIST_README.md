# پلتفرم حسابداری دستمزد — راهنمای استفاده

## اجرا
روی فایل **`PersianPayroll.exe`** دوبار کلیک کنید.
برنامه به‌صورت یک **پنجره نرم‌افزاری ویندوز** («حسابداری نیروهای نصب») باز می‌شود.

> مرورگر سیستم باز **نمی‌شود**؛ همه چیز داخل همان پنجره برنامه است.
> نیازی به نصب پایتون یا اینترنت نیست. همه چیز داخل همین پوشه است.

اگر برنامه باز نشد، فایل `desktop_error.log` کنار برنامه را بررسی کنید
(متن خطا آنجا ثبت می‌شود).

## ورود اولیه
- نام کاربری: `admin`
- رمز عبور: `admin`

پس از ورود، از **تنظیمات ← مدیریت کاربران** رمز را تغییر دهید.

## محل ذخیره اطلاعات (مسیر ثابت و امن)
اطلاعات شما **داخل پوشه برنامه نیست**، بلکه در مسیر ثابت زیر ذخیره می‌شود:

```
%LOCALAPPDATA%\PersianPayroll\payroll.db      ← دیتابیس
%LOCALAPPDATA%\PersianPayroll\backups\        ← بکاپ‌ها
```

(معمولاً چیزی شبیه `C:\Users\<نام شما>\AppData\Local\PersianPayroll`)

به همین دلیل، ساخت دوباره یا جایگزینی نسخه جدید برنامه **هرگز دیتابیس شما را پاک
نمی‌کند**. هنگام هر اجرا یک بکاپ خودکار هم ساخته می‌شود.

> اگر نسخه قدیمی دیتابیس در `...\PersianPayroll\instance\payroll.db` (کنار exe قدیمی)
> داشته باشید و مسیر جدید خالی باشد، برنامه در اولین اجرا آن را **به‌صورت خودکار
> و امن** وارد می‌کند (و هرگز روی دیتابیس دارای اطلاعات، دیتابیس خالی نمی‌نویسد).

## بکاپ و بازگردانی
- **بکاپ دستی:** تنظیمات ← بکاپ و بازگردانی ← «تهیه بکاپ جدید».
- **بازگردانی:** تنظیمات ← بکاپ و بازگردانی ← یک بکاپ را انتخاب کنید (یا مسیر یک
  فایل دیتابیس دلخواه را وارد کنید) و دکمه **«بازگردانی دیتابیس»** را بزنید.
  پیش از بازگردانی، به‌صورت خودکار از وضعیت فعلی بکاپ گرفته می‌شود.

## انتقال به کامپیوتر دیگر
علاوه بر کپی پوشه برنامه، یک بکاپ تهیه کنید و روی سیستم جدید از طریق صفحه
«بازگردانی» آن را بازگردانید (یا کل پوشه `%LOCALAPPDATA%\PersianPayroll` را کپی کنید).

## گزارش‌ها
در صفحه گزارش‌ها می‌توانید خروجی **Excel** بگیرید یا با دکمه **«PDF / چاپ»**
از طریق مرورگر فایل PDF بسازید.

برای فهرست کامل تست‌های دستی، فایل `TEST_CHECKLIST.md` را ببینید.
